/* use anyhow::Result;
use std::env; */

/* pub fn add_to_env_variable(variable: &str, value: &str) -> Result<()> {

    let mut existing_value = env::var_os(variable).unwrap_or("".into());

    println!("Found env: {:?} variable: {:?}", &variable, &existing_value);

    Ok(())
}
 */